/**
 * @name CustomTabBar
 * @description 自定义底部导航组件
 * @author darcrand
 */

export default function CustomTabBar() {
  // 内容为空
  // 但可以用于占位
  // 该文件路径必须固定为：src/custom-tab-bar/index.tsx
  return null
}
